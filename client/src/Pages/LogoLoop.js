import { isRequiredArgument } from "graphql";
import { Link } from "react-router-dom";

const Logo_Loop = () => {
  return (
    <div className="splash">
      <div className="contentbox">
        <video src="../media/Logo_Loop.mp4" />
      </div>
    </div>
  );
};

export default Logo_Loop;
